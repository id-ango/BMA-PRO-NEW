@page "/ModuleTestRun/TransTestRun"
@using eSoft.Piutang.Model;
@using eSoft.Piutang.Services;
@using eSoft.TestRun.Model
@using eSoft.TestRun.Services

@inject IReceivableServices serviceAR
@inject ITestRunServices service
@inject NavigationManager navigationmanager

<div class="container">
    <h1>Daftar Mesin Terkirim</h1>
</div>


<div class="container">
    <NavLink href="/ModulePersediaan/AddTransTestRun" class="btn btn-primary">Add</NavLink>
</div>
<button Class="btn btn-info" @onclick="@OnAddLine">Add Line</button>
<br />
<br />


<div class="container">
    <DataGridComponent Items="Transaksi" Context="transaksi" GetFilterableText="@(item =>$"{item.NamaCustomer} {item.TsOrder} {item.Daerah} {item.Daftar}  ")">
        <TableHeader>
            <th scope="col">Customer</th>
            <th scope="col">Tanggal</th>
            <th scope="col">Daftar</th>
            <th scope="col">Keterangan</th>
            <th scope="col">Tujuan</th>
            <th scope="col">Tanggal Test</th>
            <th></th>
        </TableHeader>
        <RowTemplate>
            <td>@transaksi.NamaCustomer
               <br/>
                <span class="badge bg-primary">@transaksi.TsOrder</span>
            </td>
            <td>@transaksi.TglKirim.ToShortDateString()</td>
            <td>@transaksi.Daftar</td>
            <td>@transaksi.Keterangan</td>
            <td>@transaksi.Daerah</td>
            <td>@transaksi.TglTest.ToShortDateString()</td>
            <td>
                <button Class="btn btn-light">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-three-dots-vertical" viewBox="0 0 16 16">
                        <path d="M9.5 13a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0zm0-5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0zm0-5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z" />
                    </svg>
                </button>
                <button type="button" class="btn btn-primary">Sample Button
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-three-dots-vertical" viewBox="0 0 16 16">
                        <path d="M9.5 13a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0zm0-5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0zm0-5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0z" />
                    </svg>

                </button>
                <button @onclick="() => OpenRubah(transaksi)" class="btn btn-success">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pencil" viewBox="0 0 16 16">
                        <path d="M12.146.146a.5.5 0 0 1 .708 0l3 3a.5.5 0 0 1 0 .708l-10 10a.5.5 0 0 1-.168.11l-5 2a.5.5 0 0 1-.65-.65l2-5a.5.5 0 0 1 .11-.168l10-10zM11.207 2.5 13.5 4.793 14.793 3.5 12.5 1.207 11.207 2.5zm1.586 3L10.5 3.207 4 9.707V10h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.293l6.5-6.5zm-9.761 5.175-.106.106-1.528 3.821 3.821-1.528.106-.106A.5.5 0 0 1 5 12.5V12h-.5a.5.5 0 0 1-.5-.5V11h-.5a.5.5 0 0 1-.468-.325z" />
                    </svg>
                </button>
               @* <button @onclick="() => OpenDeleteDialog(transaksi)" class="btn btn-danger">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash" viewBox="0 0 16 16">
                        <path d="M5.5 5.5A.5.5 0 0 1 6 6v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm2.5 0a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-1 0V6a.5.5 0 0 1 .5-.5zm3 .5a.5.5 0 0 0-1 0v6a.5.5 0 0 0 1 0V6z" />
                        <path fill-rule="evenodd" d="M14.5 3a1 1 0 0 1-1 1H13v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4h-.5a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1H6a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1h3.5a1 1 0 0 1 1 1v1zM4.118 4 4 4.059V13a1 1 0 0 0 1 1h6a1 1 0 0 0 1-1V4.059L11.882 4H4.118zM2.5 3V2h11v1h-11z" />
                    </svg>
                </button>*@
                <button @onclick="() => OpenPrint(transaksi.TsScheduleId)" class="btn btn-info">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-printer" viewBox="0 0 16 16">
                        <path d="M2.5 8a.5.5 0 1 0 0-1 .5.5 0 0 0 0 1z" />
                        <path d="M5 1a2 2 0 0 0-2 2v2H2a2 2 0 0 0-2 2v3a2 2 0 0 0 2 2h1v1a2 2 0 0 0 2 2h6a2 2 0 0 0 2-2v-1h1a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-1V3a2 2 0 0 0-2-2H5zM4 3a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1v2H4V3zm1 5a2 2 0 0 0-2 2v1H2a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h12a1 1 0 0 1 1 1v3a1 1 0 0 1-1 1h-1v-1a2 2 0 0 0-2-2H5zm7 2v3a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1v-3a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1z" />
                    </svg>
                </button>
            </td>
        </RowTemplate>
    </DataGridComponent>


</div>

@* @if (DeleteDialogOpen)
{
    <ModalDialogComponent Title="Are you sure?" Text="Do you want to delete this entry?" OnClose="OnDeleteDialogClose"></ModalDialogComponent>
}

@if (NullDialogOpen)
{
    <ModalDialogComponent Title="MESSAGE" Text="Transaction Already Has Payment" TombolSave="false" OnClose="OnDeleteNullClose"></ModalDialogComponent>
} *@

@if (IsVisible)
{
    <ModalTestRunComponent SrcCode="srccode" OnClose="OnVisibleClose" OnDetail="OnDetailTrans" isEdit="IsEdit" DetailRubah="Transd" ></ModalTestRunComponent>
}

@code {

    private List<TsSchedule> Transaksi;

   // private TsSchedule _bankToDelete;

    public bool DeleteDialogOpen { get; set; }
    public bool NullDialogOpen { get; set; }
    private List<ArCust> srccode;
    public bool IsVisible { get; set; }
    public bool IsKurs { get; set; }
    public bool IsEdit { get; set; }
    public bool SearchOpen { get; set; }
    private TsSchedule Transd = new ();

    protected override void OnInitialized()
    {
        base.OnInitialized();
        LoadGet();
        Loadsupplier();

    }

    private void LoadGet()
    {
        Transaksi = service.GetTsSchedules();

    }

    private void OnVisibleClose()
    {

        IsVisible = false;
        IsKurs = false;
        IsEdit = false;
        StateHasChanged();

    }
    void Loadsupplier()
    {

        srccode = serviceAR.GetCustomer();

    }

    // private async Task OnDeleteDialogClose(bool accepted)
    // {
    //     if (accepted)
    //     {
    //       //  await service.DelTransH(_bankToDelete.IcTransHId);
    //         _bankToDelete = null;
    //     }

    //     DeleteDialogOpen = false;
    //     LoadGet();

    //     StateHasChanged();

    // }

    // private void OnDeleteNullClose(bool accepted)
    // {
    //     //if (accepted)
    //     //{
    //     //    await cashBank.DelBank(_bankToDelete.CbBankId);
    //     //    _bankToDelete = null;
    //     //}

    //     DeleteDialogOpen = false;
    //     NullDialogOpen = false;
    //     LoadGet();
    //     StateHasChanged();

    // }
    // private void OpenDeleteDialog(TsSchedule bank)
    // {

    //     DeleteDialogOpen = true;


    //     _bankToDelete = bank;
    //     StateHasChanged();
    // }

    private void OpenRubah(TsSchedule xKdBank)
    {
        IsEdit = true;
        IsVisible = true;
        StateHasChanged();
    }

    private void OpenPrint(int xKdBank)
    {
        navigationmanager.NavigateTo($"/ModulePrinting/PrintSuratMutasi/{xKdBank}");
    }

    private void OnAddLine()
    {

        this.IsVisible = true;


        StateHasChanged();

        //IsEdit = false;
        //ShowOnClick();
    }
    private void OnDetailTrans(TsSchedule transD)
    {
        if (!IsEdit)
        {
            Transaksi.Add(transD);
        }

        IsVisible = false;
        IsKurs = false;
        IsEdit = false;
        StateHasChanged();
    }
}
